<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.investment.survey.domain.repository.log.ActivityLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO tbl_activity_log (
             user_id
           , action_type
           , target_type
           , target_id
           , detail
           , ip
           , user_agent
           , reg_dt
        ) VALUES (
             #{userId}
           , #{actionType}
           , #{targetType}
           , #{targetId}
           , #{detail}
           , #{ip}
           , #{userAgent}
           , NOW()
        )
    </insert>

</mapper>
