<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.investment.portal.domain.repository.log.ActivityLogMapper">

    <!-- 컬럼 순서 = ActivityLog 필드 순서 (생성자 자동 매핑) -->
    <sql id="columns">
        log_id, user_id, action_type, target_type, target_id, detail, ip, user_agent, reg_dt
    </sql>

    <insert id="insert" useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO tbl_activity_log (
             user_id
           , action_type
           , target_type
           , target_id
           , detail
           , ip
           , user_agent
           , reg_dt
        ) VALUES (
             #{userId}
           , #{actionType}
           , #{targetType}
           , #{targetId}
           , #{detail}
           , #{ip}
           , #{userAgent}
           , NOW()
        )
    </insert>

    <!-- 사용자 본인 로그 (페이지) -->
    <select id="findByUser" resultType="ActivityLog">
        SELECT <include refid="columns"/>
          FROM tbl_activity_log
         WHERE user_id = #{userId}
         ORDER BY reg_dt DESC, log_id DESC
         LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countByUser" resultType="long">
        SELECT COUNT(*) FROM tbl_activity_log WHERE user_id = #{userId}
    </select>

    <!-- 관리자 전체 조회 (userId/actionType 선택 필터) -->
    <!--
        excludeUserId: user_id 가 NULL(익명/시스템)인 행까지 걷어내지 않도록
        "IS NULL OR !=" 로 쓴다 — NULL != 값 은 SQL에서 항상 NULL(=false 취급)이라
        단순 != 만 쓰면 시스템 액션이 통째로 결과에서 빠진다.
    -->
    <select id="search" resultType="ActivityLog">
        SELECT <include refid="columns"/>
          FROM tbl_activity_log
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            <if test="actionType != null and actionType != ''">
                AND action_type = #{actionType}
            </if>
            <if test="excludeUserId != null and excludeUserId != ''">
                AND (user_id IS NULL OR user_id != #{excludeUserId})
            </if>
        </where>
         ORDER BY reg_dt DESC, log_id DESC
         LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countSearch" resultType="long">
        SELECT COUNT(*)
          FROM tbl_activity_log
        <where>
            <if test="userId != null and userId != ''">
                AND user_id = #{userId}
            </if>
            <if test="actionType != null and actionType != ''">
                AND action_type = #{actionType}
            </if>
            <if test="excludeUserId != null and excludeUserId != ''">
                AND (user_id IS NULL OR user_id != #{excludeUserId})
            </if>
        </where>
    </select>

</mapper>
